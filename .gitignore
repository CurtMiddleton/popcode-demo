# Dependencies — installed by Vercel from package.json/package-lock.json at deploy.
# (Some node_modules files were committed historically; new installs stay out of commits.)
node_modules

# Sentry build/source-map tooling (mirrors the Bashō setup)
.env.sentry-build-plugin
.sentryclirc

# Local env files (DSN/keys live in Vercel env, not the repo)
.env
.env.local
.env*.local

# OS cruft
.DS_Store
