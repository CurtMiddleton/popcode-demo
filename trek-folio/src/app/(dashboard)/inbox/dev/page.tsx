import { DevParseTester } from "./dev-parse-tester";

export const metadata = {
  title: "Email parse tester — Trek Folio",
};

export default function DevParsePage() {
  return <DevParseTester />;
}
